# Viruneの開発に参加する

[英語版](CONTRIBUTING.md)

Viruneへのコントリビューションを歓迎します。

ここでは、開発環境の準備から実装、テスト、Pull Requestまでの基本的な流れをまとめています。

## 変更を始める前に

プロジェクト内でのやり取りは[行動規範](CODE_OF_CONDUCT_ja.md)に従ってください。

セキュリティ上の問題は公開Issueへ書かず、[セキュリティポリシー](SECURITY_ja.md)に従って報告してください。

誤字修正や小さなバグ修正であれば、Issueを作らずにPull Requestを送っても構いません。

一方、以下のような変更は影響が大きいため、実装を始める前にIssueで目的や変更範囲を相談してください。

- 大きな機能追加や設計変更
- 言語の構文や型のルールを変える変更
- 外部ツールから利用できるCompiler APIの変更
- RuntimeやJavaScript相互運用との接続方法を変える変更
- 既存の利用者との互換性に影響する変更
- CIやリリースの安全性に関わる変更

大きな機能を複数のPull Requestに分ける場合は、全体を追跡するIssueと個別の実装Issueを分けることがあります。

既存のIssueに対する変更であれば、Pull RequestからそのIssueを参照してください。

## 開発環境

必要なNode.jsのバージョンは、ルート`package.json`の`engines`に記載しています。

リポジトリを取得したら、以下を実行します。

```bash
git clone https://github.com/yaona807/virune.git
cd virune
npm run bootstrap
npm run build
```

`npm run bootstrap`は、ロックファイルに従ってViruneの開発に必要な依存関係をインストールします。

セットアップ後、CLIが起動することは以下で確認できます。

```bash
npm run virune -- --version
```

コンパイラやランタイムの主要なテストは以下で実行できます。

```bash
npm run test:core
```

ほかの開発用コマンドやテストは、ルート`package.json`の`scripts`を確認してください。

## リポジトリの構成

主なディレクトリは以下のとおりです。

| パス | 主な役割 |
|---|---|
| `packages/compiler` | Viruneのコードを解析し、型を検査してJavaScriptを生成するコンパイラ |
| `packages/runtime` | 生成したJavaScriptの実行時に使う処理 |
| `packages/stdlib` | Viruneから利用できる標準ライブラリ |
| `packages/formatter` | Viruneコードのフォーマッター |
| `packages/js-interop` | JavaScript / TypeScriptのライブラリと接続する処理 |
| `packages/cli` | `virune`コマンド |
| `packages/language-server` | エディタの補完やエラー表示を提供するLanguage Server |
| `packages/vscode` | VS Code拡張 |
| `spec` | Viruneの言語仕様とRuntime ABI |
| `conformance` | 言語仕様どおりに動くことを確認するテスト |
| `integration` | 複数のコンポーネントを組み合わせたテスト |
| `selfhost` | Viruneで実装しているセルフホスト用コンパイラ |
| `.github` | GitHub Actions、CI、リリースなどの設定 |
| `scripts` | ビルドや検証に使うスクリプト |

### コンパイラの流れ

コンパイラを変更する場合は、変更したい処理をどの段階が担当しているかを見ると追いやすくなります。

大まかな流れは以下です。

```text
Viruneのソースコード
  ↓
字句解析 / 構文解析
  ↓
AST
  ↓
名前解決 / 型検査
  ↓
HIR / MIR
  ↓
JavaScript / Source Map
```

ASTは、解析したソースコードの構造を表します。

その後、名前や型などを検査し、HIRやMIRと呼ばれるコンパイラ内部の中間表現へ変換してからJavaScriptを生成します。

AST、HIR、MIRなどはコンパイラ内部で使うもので、Stable Compiler APIとして直接公開しません。

## 変更の進め方

基本的な流れは以下です。

1. 関連するIssueやPull Requestを確認する
2. 最新の`main`から作業ブランチを作る
3. 実装し、変更した部分に近いテストを実行する
4. 動作を変更した場合は、必要に応じて回帰テストを追加する
5. 言語仕様や公開API / ABIに影響する場合は、関連する仕様やスナップショットも更新する
6. 必要なテストを実行してPull Requestを作成する

CIやレビューで問題が見つかった場合は修正し、影響するテストをもう一度実行してください。

Pull Requestには、関係のない整形、リファクタリング、命名変更をできるだけ混ぜないようにします。

### スタックしたPull Request

ある変更が別のPull Requestに依存している場合は、PRをスタックしても構いません。

独立して実装やテストができる変更は、それぞれ`main`から作成してください。

スタックは原則として親PRと子PRの2段までとします。

親PRがマージされたら、子PRのbaseを`main`へ変更し、必要に応じて最新の`main`を取り込みます。

## テスト

まず変更した部分に近いテストを実行し、その後、変更範囲に応じて確認範囲を広げます。

よく使うコマンドは以下です。

| 変更内容 | 主なコマンド |
|---|---|
| TypeScriptの型エラーやビルドを確認したい | `npm run check` |
| コンパイラやランタイムを変更した | `npm run test:core` |
| 言語の構文や型のルールを変更した | `npm run spec:check` |
| 外部向けCompiler APIを変更した | `npm run api:check` |
| RuntimeやInteropの公開インターフェースを変更した | `npm run abi:check` |
| リポジトリ全体を広く確認したい | `npm run verify` |

変更内容によっては、このほかのテストも必要です。利用できるコマンドは`package.json`の`scripts`にあります。

バグ修正では、できるだけ修正前に失敗し、修正後に成功する回帰テストを追加してください。

変更内容によっては、正常系だけでなく、不正な入力、境界値、値の欠落、重複、途中失敗、後始末なども確認します。

同じ入力から毎回同じ結果になる必要がある処理では、実行順序や環境によって結果が変わらないことも確認してください。

テストを通すためだけに特定の入力を特別扱いしたり、安全性や既存のチェックを弱めたりしないでください。

## 影響の大きい変更

ここからは、該当する部分を変更する場合に確認してください。

### 言語仕様

Viruneの構文や型のルールは[言語仕様](spec/README_ja.md)にまとめています。

例えば、以下は言語仕様に関わる変更です。

- 新しい構文を追加する
- 既存の構文の意味を変える
- 型検査のルールを変える
- `Option`や`Result`など、言語として決められている動作を変える

このような変更では、実装とテストだけでなく、関連する仕様も同じPull Requestで更新してください。

仕様と実装、テストの内容が食い違っていることに気付いた場合は、そのまま片方に合わせるのではなく、Issueや既存の仕様を確認して意図を整理してから修正します。

### Compiler API

Viruneのコンパイラは、CLIだけでなく外部のツールから利用するためのAPIも公開しています。これをCompiler APIと呼んでいます。

公開している関数や型は、`packages/compiler/api/stable-api.snapshot.json`にも記録しています。このファイルは、公開APIが意図せず変わっていないかを確認するために使います。

Compiler APIを変更した場合は、以下を実行します。

```bash
npm run api:check
```

例えば、公開している関数を削除したり、引数や戻り値の型を変えたりすると、既存のツールが動かなくなる可能性があります。

そのため、`api:check`が失敗したときに、チェックを通すためだけにスナップショットを現在の実装へ合わせることはしません。

公開APIを意図的に変更する必要がある場合は、[互換性方針](COMPATIBILITY_ja.md)を確認し、Issueで変更理由や既存利用者への影響を整理してください。

### Runtime ABI / Interop ABI

Viruneが生成したJavaScriptは、Runtimeの関数を呼び出して動きます。また、JavaScriptのライブラリと値を受け渡す場合にも、決められた形式があります。

このような、コンポーネント同士を接続するためのルールをABIと呼んでいます。

Viruneでは主に、

- 生成コードとRuntimeを接続するRuntime ABI
- ViruneとJavaScript相互運用を接続するInterop ABI

があります。

これらを変更すると、以前に生成したコードや既存の相互運用コードが動かなくなる可能性があります。

公開ABIに関わる変更では、`packages/public-abi.snapshot.json`と[Runtime ABI v2](spec/runtime-abi_ja.md)を確認してください。

```bash
npm run abi:check
```

既存の利用者との互換性に影響する場合は、[互換性方針](COMPATIBILITY_ja.md)も確認します。

### セルフホスティング

Viruneには、Viruneコンパイラ自身をViruneで実装するセルフホスティングの仕組みがあります。

ただし、コンパイラに関わるすべての処理をViruneへ移すわけではありません。

例えば、字句解析、構文解析、型検査のように、入力を受け取って結果を返せる処理はVirune側で実装できます。

一方、以下のような実行環境に依存する処理はJavaScript / TypeScript側で扱います。

- ファイルの読み書き
- パス解決
- プロセスや環境変数
- TypeScript宣言の解析
- CLIやエディタとの通信
- パッケージ作成やリリース処理

この境界を崩すとセルフホスティングのためだけに言語や公開APIが複雑になるため、セルフホスティングを実装しやすくすることだけを理由に、Viruneの言語仕様、Compiler API、Runtime ABI、Interop ABI、公開標準ライブラリを変更しないでください。

現在のセルフホスティング実装や検証方法は、`.github/self-hosting/`、`selfhost/`、関連するスクリプトとテストを確認してください。

既存のセルフホスティング用チェックを回避しないでください。

### リリース

通常のコード変更では、この節を意識する必要はありません。

リリース条件や公開処理を変更する場合は、通常のCIに加えてリリース用の検証にも影響します。

関連する設定や処理は以下にあります。

- `.github/stable-release-gate.json`
- `.github/release/`
- リリース関連のGitHub Actions
- リリース関連の検証スクリプト

リリース用のチェックを弱めたり、回避したりしないでください。

また、すでに公開されているリリース成果物を、別の内容で上書きしないでください。

## CI

Pull Requestを作成すると、変更内容に応じてGitHub Actionsが実行されます。

CIの結果は、そのCIを実行したコミットに対する結果です。CIの後にコードを変更した場合は、変更後のコミットでも必要なチェックが成功していることを確認してください。

CIが失敗した場合は、まずログを確認します。

コードやテストが原因であれば修正します。

GitHub Actionsやランナーなど、一時的な基盤障害であることが確認できた場合は、同じコミットで再実行して構いません。

原因を確認せず、成功するまで繰り返し再実行することは避けてください。

## 文書を変更する場合

人が継続して管理する文書は、必要以上に増やさない方針です。

新しいMarkdownファイルを作る前に、README、CONTRIBUTING、COMPATIBILITY、`spec/`など既存の文書へ追加できないか確認してください。

特定のIssueやPull Requestでしか使わない説明は、そのIssueやPull Requestに残します。

日本語版と英語版が対になっている文書では、内容やルールを揃えてください。

直訳することより、それぞれの言語で自然に読めることを重視します。

同じ説明を複数の文書へコピーするより、必要に応じて関連する文書へリンクしてください。

## Pull Request

Pull Requestは、できるだけ1つの目的に絞ります。

本文には、変更内容に応じて以下を書いてください。

- 何を変更したか
- 関連するIssue
- 変更した理由
- 実行したテスト
- 互換性や安全性への影響がある場合はその内容

実装途中や、必要なテストがまだ終わっていない場合はDraft Pull Requestを利用できます。

レビューでは、期待した動作になっているか、必要なテストがあるか、既存の互換性を壊していないかを確認します。

エラーや途中失敗を適切に扱えているか、関係のない変更やデバッグ用コードが残っていないかも確認してください。

修正した場合は、その変更によって影響するテストをもう一度実行します。

マージ前には、必要なCIが成功していること、未解決のレビューコメントがないこと、意図しない差分が含まれていないことを確認してください。

Viruneでは原則としてsquash mergeを使用します。

関連Issueは、Pull Requestがマージされたことだけでは閉じず、そのIssueで必要としていた内容が`main`へ反映されたことを確認してから閉じます。

## ライセンス

Viruneは[Apache License 2.0](LICENSE)で公開されています。

Viruneへ取り込む目的で提出したコードや文章は、別途明示しない限りApache License 2.0の条件で提供されるものとして扱います。

第三者のコード、文章、画像などを含める場合は、その利用条件と必要な表示を確認してください。
