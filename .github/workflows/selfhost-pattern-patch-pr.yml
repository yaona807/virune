name: Self-host pattern integration PR

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'agent/selfhost-match-pattern-ast'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: agent/selfhost-match-pattern-ast
      - name: Apply parser integration patch
        run: python3 scripts/apply-selfhost-pattern-patch.py
      - name: Commit parser integration
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add selfhost/mvp/src/frontend-parser-core.virune
          git commit -m "compiler(selfhost): integrate match and pattern parser"
          git push origin HEAD:agent/selfhost-match-pattern-ast
