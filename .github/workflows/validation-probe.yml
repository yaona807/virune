name: Workflow validation probe

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  probe:
    name: Validate workflow scheduling
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Confirm runner scheduling
        run: echo 'Workflow validation and runner scheduling succeeded.'
