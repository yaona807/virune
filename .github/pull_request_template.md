## Summary

<!-- What does this PR change, and why? -->

## Implementation issue

Refs #

<!-- Required for a normal implementation PR. Reference an Issue whose `Work item role` is `Implementation`. Use plain `Refs`; do not use `Closes`, `Fixes`, `Resolves`, or a GitHub closing relationship as the normal completion mechanism. -->

## Tracking / parent issues

<!-- Use plain `Refs #...` for any Tracking parent, or write `None`. A Tracking Issue is not a substitute for the Implementation Issue above. -->

## Scope

<!-- List the files/subsystems or behavioral boundaries intentionally changed. -->

## Non-goals / invariants

<!-- What intentionally does not change? Include safety, API/ABI, or semantic invariants when relevant. -->

## Validation

- [ ] Used repository-owned validation where available
- [ ] Added or updated tests for behavior changes
- [ ] Added a regression test for a bug fix when practical
- [ ] Checked relevant negative/boundary/failure cases

Commands / evidence:

```text
# command -> result
```

<!-- GitHub is authoritative for the mutable current PR base/head. Do not maintain copied `current head` / `current base` fields here. When citing formal CI or other commit-specific evidence, identify the immutable exact SHA that the evidence belongs to. -->

## Compatibility / safety impact

<!-- Language Specification, Compiler API, Runtime ABI, Interop ABI, Node/browser targets, safety boundaries, determinism/reproducibility. Write "None" only after checking. -->

## Review readiness

- [ ] This PR is one logical change
- [ ] Required public documentation is synchronized when applicable
- [ ] No unrelated refactor or temporary workaround is included
- [ ] A complete adversarial review pass found zero actionable findings
- [ ] Any CI evidence cited belongs to the actual current PR head
- [ ] This PR is not validation-only or superseded

## Stack / superseded relationship

<!-- State dependencies/replacement PRs, or "None". Stacked PRs are exceptional; recommended max depth is 2. -->
