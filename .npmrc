# Install third-party dependencies from the public npm registry. Virune packages are distributed through GitHub Releases.
registry=https://registry.npmjs.org/
# Do not rewrite package-lock.json dist URLs to a user- or machine-level registry.
replace-registry-host=never
