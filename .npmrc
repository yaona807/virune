# Virune is a public OSS project. Always install dependencies from the public npm registry.
registry=https://registry.npmjs.org/
# Do not rewrite package-lock.json dist URLs to a user- or machine-level registry.
replace-registry-host=never
