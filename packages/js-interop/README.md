# @virune/js-interop

Versioned TypeScript-backed provider used by the Virune compiler and language server to inspect JavaScript package declarations without importing TypeScript's type model into Virune's native type system.
