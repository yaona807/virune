# 互換性・非推奨化ポリシー

[English version](COMPATIBILITY.md)

この文書では、Viruneの安定版でどの互換性を守るかを定めます。

## 互換性の区分

Viruneでは、公開仕様や内部実装を次の3つに分けます。

- **安定（Stable）**: 安定版で互換性を維持する公開仕様
- **実験的（Experimental）**: 評価中で、互換性を保証しない公開仕様
- **内部（Internal）**: 互換性保証の対象外となる内部実装

### 安定（Stable）

主な対象は次のとおりです。

- [言語仕様](spec/)で定めるVirune言語の挙動
- 文書化された`virune.json`の設定
- 標準ライブラリの公開宣言と`package.json`の`exports`設定
- `@virune/compiler`のルート公開API
- 安定と明示したRuntime ABIとInterop ABI
- 文書化されたCLIの挙動
- 診断コードとJSON形式で定める公開仕様と、安定と明示したその他の機械可読形式
- 安定版向けに文書化されたLSP／VS Codeの公開機能、設定、公開コマンド
- Node.jsやVS Codeなど、安定版で対応すると定めた環境

既存の利用方法を壊さない追加や修正はできます。
意図的な非互換変更は、後述する例外を除きメジャーリリースで行います。

バージョン番号、バージョン付きのパス、スナップショットが存在するだけでは安定扱いにはなりません。安定扱いにする場合は明示し、スナップショットを更新しただけで非互換変更を認めることはありません。

LSPは、Viruneが対応すると定めたVS Code APIとLanguage Server Protocolに従います。

公開済みの診断コードは、そのスキーマバージョン内で安定した識別子として扱います。既存の`Lxxxx`コードを別の意味の診断へ再利用してはいけません。外部ツールはメッセージ本文ではなく、`source`と`code`の組み合わせ、または`qualifiedCode`で診断を識別してください。現在の正確なコード範囲、診断一覧、JSONフィールド構造は、コンパイラーが管理する診断コードのソースとスキーマを正本とします。

### 実験的（Experimental）

実験的な公開仕様は安定版の互換性保証の対象外で、どのリリースでも変更または削除できます。

`@virune/compiler/experimental`が該当します。

`Semantic Snapshot`と`Semantic Change Evidence`は、必要な評価を終えたうえで明示的に安定扱いへ移すまで実験的です。評価を終えただけでは安定扱いになりません。

実験的な公開仕様を利用しても、無関係な安定仕様の保証には影響しません。

### 内部（Internal）

コンパイラー内部、セルフホスティングの内部実装、キャッシュ、CI用データ、リポジトリ専用コマンド、未文書化のパッケージ内パスなどが該当します。

安定した公開仕様を壊さない限り、事前の非推奨化なしで変更できます。

## バージョンと非互換変更

安定版のバージョン付けにはSemantic Versioningを使います。

- **Patch**: 後方互換な修正
- **Minor**: 既存の挙動を維持した追加や改善
- **Major**: 安定した仕様への意図的な非互換変更

プレリリースでは非互換変更があり得ます。
ナイトリーには互換性保証がありません。

次のような変更は非互換です。

- 公開API、ABI、標準ライブラリ、CLI、エディタ機能を削除・名前変更したり、互換性なく変更する
- 以前有効だった公開設定を使えなくしたり、その意味や既定値を互換性なく変更する
- 以前は仕様に適合していたViruneプログラムを構文解析、型検査、リンク、実行できなくしたり、その意味を互換性なく変更する
- 安定扱いの診断や機械可読形式を互換性なく変更する
- 最低対応バージョンを引き上げ、以前の対応環境を対象外にする

文言、色、レイアウトなど、人向けの表示と、未文書化の設定・JSON項目・内部仕様は、明示的に保証しない限り安定仕様には含めません。

## 非推奨化

安定した仕様を削除または非互換変更する場合は、原則として次の順序で進めます。

1. 非推奨であることを公開文書に明記し、実用的な場合はツールや型情報にも反映する
2. サポート対象の代替手段または移行方法を示す
3. 旧仕様を利用できる状態で、非推奨化を含む安定版を少なくとも1回公開する
4. メジャーリリースで変更し、移行方法を案内する

移行方法はリリース前に用意し、対象バージョンと仕様、変更前後の内容、必要な移行手順を示します。

実験的な公開仕様と内部実装には、この手順を要求しません。
非推奨化を理由に、型、安全性、ABI、検証の境界を弱めてはいけません。また、非推奨としただけで既存プログラムの意味を変えてはいけません。

## 正しさ・安全性・セキュリティ上の例外

規範仕様に違反していると分かっている挙動を、互換性のためだけに維持してはいけません。

重大な正しさ・安全性・セキュリティ上の問題は、後方互換のまま直せるならその方法を優先します。後方互換のまま直す現実的な方法がなく、修正しなければ問題を残す場合に限り、メジャーリリースを待たずに非互換修正できます。

Node.jsなどのサポート終了により、従来の環境を安全または現実的に維持できない場合も同様です。

例外的な非互換変更では、影響する安定仕様、従来の挙動、後方互換にできない理由、緩和策または移行方法を明示し、無関係な安定仕様は維持します。

既存の言語仕様に実装を戻す修正は、言語仕様の変更ではありません。ただし、安定した仕様と非互換になる場合は、この例外条件を満たすか、次のメジャーリリースまで待ちます。

この例外を通常の互換性ルールの回避に使ってはいけません。

仕様、ポリシー、実装、テストに矛盾がある場合は、都合のよい解釈をせず、矛盾を解消してから安定した保証として扱います。リリースノートや移行案内だけで、言語仕様やこのポリシーを上書きすることはできません。

CIやスナップショットが成功していることだけを、非互換変更を認める理由にしてはいけません。
