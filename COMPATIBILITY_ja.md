# 互換性方針

English: [COMPATIBILITY.md](COMPATIBILITY.md)

この文書は、Viruneの安定版で守る互換性と、非互換変更を扱うときの原則を定めます。

## 互換性の区分

Viruneでは、公開範囲を次の3つに分けます。

- **Stable**: 安定版で互換性を維持する公開契約
- **Experimental**: 評価中で、互換性を保証しない公開範囲
- **Internal**: 実装詳細で、互換性保証の対象外

### Stable

Stableであることは明示されている必要があります。バージョン番号、バージョン付きのパス、スナップショットがあるだけではStableにはなりません。

現在、主に次をStableな公開契約として扱います。

- [`spec/`](spec/README_ja.md)で定める規範的な言語仕様
- 文書化された`virune.json`の設定
- 標準ライブラリの公開宣言と`package.json`の`exports`
- `@virune/compiler`のルートから公開するStable Compiler API
- Stableと明示したRuntime ABIとInterop ABI
- 公開CLIのコマンド、オプション、終了状態など、利用者向けに公開している契約
- Stableと明示した診断コード、JSONなどの機械可読形式
- 安定版向けに公開しているLanguage Server／VS Codeの機能、設定、コマンド
- Node.jsやVS Codeなど、安定版で対応すると明示した実行環境

既存の利用方法を壊さない追加や修正はできます。意図的な非互換変更は、後述する例外を除きメジャーリリースで行います。

### Experimental

Experimentalは安定版の互換性保証の対象外です。評価の結果に応じて、任意のリリースで変更または削除できます。

`@virune/compiler/experimental`はExperimentalです。Semantic SnapshotとSemantic Change Evidenceも、必要な評価を終えたうえで明示的にStable化されるまではExperimentalです。評価が完了しただけではStableになりません。

Experimentalな範囲を使っていても、無関係なStable契約の保証には影響しません。

### Internal

コンパイラー内部、Self-hostingの内部実装、キャッシュ、CI用データ、リポジトリ専用コマンド、未公開のパッケージ内パスなどはInternalです。

Stableな契約を壊さない限り、Internalは事前の非推奨化なしで変更できます。

## バージョンと非互換変更

安定版のリリースはSemantic Versioningに従います。

- **Patch**: 後方互換な修正
- **Minor**: 既存の契約を維持した追加や改善
- **Major**: Stableな契約への意図的な非互換変更

プレリリースでは非互換変更があり得ます。Nightlyには互換性保証がありません。

次のような変更は、Stableな範囲に対して行う場合は非互換変更です。

- 公開API、ABI、標準ライブラリ、CLI、エディタ連携を削除、名前変更、または互換性なく変更する
- 以前は有効だった公開設定を使えなくする、または意味や既定値を互換性なく変更する
- 仕様に適合していたViruneプログラムを構文解析、型検査、リンク、実行できなくする、または意味を互換性なく変更する
- Stableな診断や機械可読形式を互換性なく変更する
- 最低対応バージョンを引き上げ、以前の対応環境を対象外にする

文章表現、画面上の色や配置、未公開の設定や内部形式は、明示的にStableと定めない限り互換性保証の対象外です。

## 非推奨化

Stableな契約を削除または互換性なく変更する場合は、原則として次の順序で進めます。

1. 非推奨であることを明示し、実用的な場合はツールや型情報にも反映する。
2. 代替手段または移行方法を用意する。
3. 旧契約を利用できる状態で、非推奨化を含む安定版を少なくとも1回公開する。
4. メジャーリリースで変更し、対象バージョン、変更前後の内容、必要な移行手順を示す。

ExperimentalとInternalには、この手順を要求しません。

非推奨化を理由に、型、安全性、ABI、検証の境界を弱めてはいけません。また、非推奨としただけで既存プログラムの意味を変えてはいけません。

## 正しさ・安全性・セキュリティ上の例外

規範仕様に違反していると分かっている挙動を、互換性のためだけに維持してはいけません。

重大な正しさ、安全性、セキュリティ上の問題では、後方互換な修正が合理的に可能ならそれを優先します。合理的な後方互換修正がなく、問題を残すことになる場合に限り、次のメジャーリリースを待たずに非互換修正できます。

対応していた実行環境を安全または現実的に維持できなくなった場合も同様です。

例外的な非互換変更では、少なくとも次を明らかにしてください。

- 影響するStable契約
- 従来の挙動
- 後方互換にできない理由
- 利用者が取れる緩和策または移行方法

無関係なStable契約まで変更してはいけません。この例外を通常の互換性ルールを避けるために使ってはいけません。

既存実装を規範言語仕様どおりの動作へ戻す修正は、それ自体では言語仕様の変更ではありません。ただし、Stableな別の契約と非互換になる場合は、この例外条件を満たすか、メジャーリリースまで待つ必要があります。

## 正本が食い違っている場合

言語の動作は[`spec/`](spec/README_ja.md)が規範です。その他の公開契約は、それぞれの公開API、ABI、スキーマ、機械可読ポリシーなどの正本に従います。

仕様、実装、テスト、スナップショットが食い違っている場合は、都合のよい解釈を採用しないでください。矛盾を解消してからStableな保証として扱います。

リリースノートや移行案内だけで、言語仕様やこの互換性方針を上書きすることはできません。スナップショットやCIが成功したことだけでも、非互換変更を許可したことにはなりません。
