# Viruneの互換性方針

[英語版](COMPATIBILITY.md)

Viruneでは、安定版へバージョンアップしただけで、これまで動いていたコードやツールが突然使えなくなることはできるだけ避けたいと考えています。

一方で、開発中の機能や内部実装まで、すべて同じように互換性を維持するわけではありません。

この文書では、Viruneのどの部分で互換性を維持するのか、どのような変更を非互換変更として扱うのかを定めます。

## 互換性の3つの区分

Viruneでは、機能や実装を次の3つに分けています。

- **安定（Stable）**: 安定版で互換性を維持するもの
- **実験的（Experimental）**: 評価中で、今後変更または削除する可能性があるもの
- **内部（Internal）**: Virune内部でのみ使い、互換性を保証しないもの

変更するときは、まず対象がどこに含まれるかを確認します。

## 安定した機能

安定した機能は、Viruneをバージョンアップしても既存の利用方法をできるだけ維持します。

主な対象は以下です。

- [言語仕様](spec/README_ja.md)で定めているViruneの構文や動作
- 公開している`virune.json`の設定
- 標準ライブラリの公開API
- `@virune/compiler`から公開しているCompiler API
- 安定と明示しているRuntime ABIとInterop ABI
- 公開しているCLIのコマンドや動作
- 公開している診断コードやJSON形式
- 安定版として提供しているLanguage ServerやVS Code拡張の機能、設定、コマンド
- Node.jsやVS Codeなど、対応対象としている環境

既存の使い方を壊さない機能追加や修正はできます。

既存の利用方法が動かなくなる変更は、後述する例外を除きメジャーバージョンで行います。

Language Serverは、Viruneが対応対象としているVS Code APIとLanguage Server Protocolに従います。

### 何がStableなのか

公開されているものが、すべて自動的にStableになるわけではありません。

例えば、バージョン番号が付いていることや、APIの状態を記録したスナップショットファイルが存在することだけでは、互換性保証の対象とは判断しません。

Stableとして扱うものは、言語仕様、公開API、公開設定、この互換性方針などで明示します。

Experimentalと明示している機能は、公開されていてもStableではありません。

判断に迷った場合は、その機能が利用者向けにどのように公開されているかを確認してください。

### 診断コード

`Lxxxx`形式の公開済み診断コードは、外部ツールからエラーの種類を判定するためにも使われます。

そのため、一度公開した診断コードを後から別の意味のエラーへ使い回しません。

診断メッセージの文章は改善によって変わることがあります。外部ツールから診断を識別する場合は、メッセージ本文ではなく`source`と`code`、または`qualifiedCode`を利用します。

現在の診断コードやJSON形式は、コンパイラ内の診断定義と関連するスキーマで確認できます。

## 実験的な機能

実験的な機能は、将来の安定化に向けて評価している段階です。

安定版に含まれていても、リリース間で変更または削除する可能性があります。

現在は、例えば以下が実験的な公開機能です。

- `@virune/compiler/experimental`
- コンパイラの意味解析結果を検証する`Semantic Snapshot`
- その変更内容を表す`Semantic Change Evidence`

これらは、評価が終わっただけではStableになりません。安定した公開仕様として扱うことを明示した時点から、互換性保証の対象になります。

実験的な機能を利用していても、それを理由に無関係なStable機能の互換性保証までなくなることはありません。

## 内部実装

Virune内部だけで使う仕組みは、互換性保証の対象外です。

例えば以下が該当します。

- コンパイラ内部のデータ構造
- セルフホスティングの内部実装
- キャッシュ
- CI専用のデータ
- リポジトリ内だけで利用するコマンド
- `package.json`の`exports`から公開していないパッケージ内部のファイル

これらは、Stableな機能を壊さない範囲で変更できます。

内部実装を変更するたびに、利用者向けの非推奨期間を設ける必要はありません。

## どのような変更が互換性を壊すのか

Viruneの安定版ではSemantic Versioningを使います。

- **Patch**: 既存の使い方を壊さない修正
- **Minor**: 既存の使い方を維持した機能追加や改善
- **Major**: Stableな機能への意図的な非互換変更

例えば、以下は非互換変更です。

- 公開APIの関数を削除する
- 公開APIの名前、引数、戻り値を互換性なく変更する
- 生成したJavaScriptとRuntimeの接続方法を互換性なく変更する
- ViruneとJavaScriptの値の受け渡し方を互換性なく変更する
- 標準ライブラリの既存APIを削除する
- 既存のCLIコマンドを使えなくする
- Language ServerやVS Code拡張の公開機能、設定、コマンドを互換性なく変更する
- 以前利用できた`virune.json`の設定を削除したり、その意味や既定値を互換性なく変えたりする
- 以前は正しく動いていたViruneコードをコンパイル・実行できなくしたり、実行時の意味を互換性なく変えたりする
- 公開済みの診断コードや安定したJSON形式を互換性なく変更する
- Node.jsなどの最低対応バージョンを引き上げ、以前の対応環境を対象外にする

上記のうち、生成コードとRuntimeの接続ルールをRuntime ABI、ViruneとJavaScript相互運用の接続ルールをInterop ABIと呼んでいます。

プレリリースでは、安定版になる前の調整として非互換変更を行う場合があります。

Nightly版は開発中の状態を提供するため、リリース間の互換性は保証しません。

文言、色、レイアウトなどの表示上の変更や、公開していない設定、JSON項目、内部実装は、明示的に保証していない限りStableには含めません。

## 機能を非推奨にする場合

Stableな機能を将来削除したり、互換性のない形へ変更したりする場合は、いきなり利用できなくすることは避けます。

原則として、次の順序で進めます。

1. 将来廃止することを案内する
2. 代わりに利用できる方法や移行方法を用意する
3. 旧機能をまだ利用できる状態で、非推奨化を含む安定版を少なくとも1回公開する
4. メジャーバージョンで旧機能を削除または変更する

移行が必要な場合は、何が変わるのか、どのバージョンが対象なのか、利用者が何を変更すればよいのかを案内します。

実験的な機能や内部実装には、この手順は必要ありません。

非推奨にした機能でも、実際に削除されるまでは、型チェックやABI、既存の検証を含めて従来の契約を維持します。非推奨にしたことだけを理由に、既存プログラムの意味を変えることもありません。

## 正しさや安全性を優先する場合

互換性は重要ですが、明らかに誤った動作や安全ではない動作を残す理由にはしません。

例えば、実装が[言語仕様](spec/README_ja.md)と異なっていることが分かった場合、「これまでそう動いていたから」という理由だけで誤った動作を維持することはしません。

重大な正しさ、安全性、セキュリティ上の問題が見つかった場合は、まず既存の利用方法を壊さずに修正できないかを検討します。

それが現実的に難しく、修正しなければ問題を残す場合は、メジャーバージョンを待たずに非互換な修正を行うことがあります。

Node.jsなどの外部環境がサポート終了となり、安全または現実的に維持することが難しくなった場合も同様です。

例外的に互換性を壊す場合は、少なくとも次の内容を利用者へ説明します。

- 何が影響を受けるのか
- 以前はどのように動いていたのか
- なぜ互換性を維持したまま修正できないのか
- どのように移行すればよいのか

問題と関係のないStableな機能まで一緒に変更することは避けます。

この例外は、通常の非互換変更を早く入れるための仕組みではありません。

## 仕様と実装が食い違っている場合

言語仕様、互換性方針、実装、テストが食い違っている場合は、まずどの動作を意図していたのか確認します。

実装を既存の言語仕様どおりに修正すること自体は、言語仕様の変更ではありません。

ただし、その修正によってStable版の利用者に非互換な影響が出る場合は、上記の正しさや安全性に関する例外を満たすか、次のメジャーバージョンまで待つ必要があります。

リリースノートや移行案内だけで、[言語仕様](spec/README_ja.md)やこの互換性方針の内容を変更することはできません。

CIやスナップショットのチェックが成功していることだけを理由に、非互換変更を問題ないものとして扱うこともできません。

## 変更するときに迷ったら

通常は、次のように考えれば十分です。

Stableとして公開している機能を壊す可能性があるなら、互換性への影響を確認します。

Experimentalと明示している機能であれば変更できますが、利用者がいることを考えて必要な案内は行います。

内部実装だけの変更であれば、Stableな機能の動作を壊していないことを確認します。

どこに当てはまるか判断できない場合は、実装を進める前にIssueで相談してください。

実際の開発手順やテストについては、[コントリビューションガイド](CONTRIBUTING_ja.md)を参照してください。
